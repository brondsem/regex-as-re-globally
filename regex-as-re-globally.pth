import re, sys; NOFLAG = getattr(re, 'NOFLAG', None); exec('try: import regex\nexcept ModuleNotFoundError: pass'); sys.modules['re'] = sys.modules.get('regex') or sys.modules['re']; import fnmatch, pathlib, urllib.parse, typing, tokenize, locale, glob; fnmatch.re = pathlib.re = urllib.parse.re = typing.re = tokenize.re = locale.re = glob.re = sys.modules['re']; sys.modules['re'].NOFLAG = NOFLAG

import re, sys; import itertools, pathlib; 'itertools & pathlib import simulates other .pth like __editable___*_finder files that import pathlib'; NOFLAG = getattr(re, 'NOFLAG', None); exec('try: import regex\nexcept ModuleNotFoundError: pass'); sys.modules['re'] = sys.modules.get('regex') or sys.modules['re']; import fnmatch, urllib.parse, tokenize, locale, glob; fnmatch.re = pathlib.re = urllib.parse.re = tokenize.re = locale.re = glob.re = sys.modules['re']; sys.modules['re'].NOFLAG = NOFLAG

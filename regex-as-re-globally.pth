import re, sys, fnmatch, pathlib, urllib.parse, typing, tokenize, locale, glob; NOFLAG = getattr(re, 'NOFLAG', None); exec('try: import regex\nexcept ModuleNotFoundError: pass'); sys.modules['re'] = fnmatch.re = pathlib.re = urllib.parse.re = typing.re = tokenize.re = locale.re = glob.re = sys.modules.get('regex') or sys.modules['re']; sys.modules['re'].NOFLAG = NOFLAG
